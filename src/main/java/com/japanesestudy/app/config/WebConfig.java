package com.japanesestudy.app.config;

/**
 * Intentionally left non-configured.
 *
 * CORS is configured centrally via Spring Security (see WebSecurityConfig) to
 * avoid conflicting behavior between MVC and the security filter chain.
 */
public class WebConfig {
}
